<!DOCTYPE html>

<title>MeetingPoint</title>

{% block body %}
    <h1>MeetingPoint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ meeting_point.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ meeting_point.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_meeting_point_index') }}">back to list</a>

    <a href="{{ path('app_meeting_point_edit', {'id': meeting_point.id}) }}">edit</a>

    {{ include('meeting_point/_delete_form.html.twig') }}
{% endblock %}
