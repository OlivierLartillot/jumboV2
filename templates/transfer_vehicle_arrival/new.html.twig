{{ include('partials/header-reproupement.html.twig') }}

    {{ include('partials/page-title.html.twig', {pagetitle: 'Client Card'|trans, title: 'Add Transfer Arrival'|trans}) }}
        <a href="{{ app.request.server.get('HTTP_REFERER') }}" <a class="btn btn-secondary btn-sm my-3">back to client</a>
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <div class="row justify-content-center">
                            <div class="col-md-4">
                                <h3 class="mb-3">{% trans %}Add transfer Arrival{% endtrans %}</h3>
                                {{ include('transfer_vehicle_arrival/_form.html.twig') }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>                                        

{{ include('partials/footer-regroupement.html.twig') }}