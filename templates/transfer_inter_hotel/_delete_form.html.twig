<form method="post" action="{{ path('app_transfer_inter_hotel_delete', {'id': transfer_inter_hotel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transfer_inter_hotel.id) }}">
    <button class="btn">Delete</button>
</form>
