{{ include('partials/header-reproupement.html.twig') }}

    {{ include('partials/page-title.html.twig', {pagetitle: 'General Settings', title: 'Edit Default Airport'|trans}) }}

    <a class="btn btn-secondary btn-sm my-3" href="{{ path('app_user_list') }}">{% trans %}back to list{% endtrans %}</a>

        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <div class="row justify-content-center">
                            <div class="col-md-4">
                                {{ include('user/_form.html.twig', {'button_label': 'Update'|trans}) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
            <button class="btn btn-danger waves-effect waves-light">{% trans %}Delete{% endtrans %}</button>
        </form>
{{ include('partials/footer-regroupement.html.twig') }}
