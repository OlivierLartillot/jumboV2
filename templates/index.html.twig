{{ include('partials/header-reproupement.html.twig') }}

                        {{ include('partials/page-title.html.twig', {pagetitle: 'Minible', title: 'Bienvenue Sur le Kino Dashboard'}) }}

                        <div class="row">
                            <div class="col">
                                <div class="card">
                                    <div class="card-body">
                                        <div class="float-end mt-2">
                                            <div id="total-revenue-chart"></div>
                                        </div>
                                        <div>
                                            <h4 class="mb-1 mt-1">Importation du CSV</h4>
                                            <form action="{{ path('admin_traitement_csv') }}" method="post" enctype="multipart/form-data">
                                                Seleccione el archivo de Excel para importar :
                                                <input type="file" name="fileToUpload" id="fileToUpload">
                                                <input type="hidden" name="token" value="{{ csrf_token('add-csv') }}"/>
                                                <input type="submit" value="Importar" name="submit">
                                            </form>
                                        </div>
                                        <p class="text-muted mt-3 mb-0"><span class="text-success me-1"><i class="mdi mdi-arrow-up-bold me-1"></i>2.65%</span> since last week
                                        </p>
                                    </div>
                                </div>
                            </div> <!-- end col-->
                        </div>


{{ include('partials/footer-regroupement.html.twig') }}




