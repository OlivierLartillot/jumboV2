<!DOCTYPE html>

<title>Edit TransferJoan</title>

{% block body %}
    <h1>Edit TransferJoan</h1>

    {{ include('transfer_joan/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_transfer_joan_index') }}">back to list</a>

    {{ include('transfer_joan/_delete_form.html.twig') }}
{% endblock %}
