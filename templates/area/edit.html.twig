{{ include('partials/header-reproupement.html.twig') }}

    {{ include('partials/page-title.html.twig', {pagetitle: 'General Settings', title: 'Edit Area'|trans}) }}

    <a class="btn btn-secondary btn-sm my-3" href="{{ path('app_area_index') }}">{% trans %}back to list{% endtrans %}</a>

        

        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        {{ include('area/_form.html.twig', {'button_label': 'Update'|trans}) }}
                    </div>
                </div>
            </div>
        </div>

        {{ include('area/_delete_form.html.twig') }}
            
{{ include('partials/footer-regroupement.html.twig') }}

