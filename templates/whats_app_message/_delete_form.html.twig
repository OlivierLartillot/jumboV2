<form method="post" action="{{ path('app_whats_app_message_delete', {'id': whats_app_message.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ whats_app_message.id) }}">
    <button class="btn">Delete</button>
</form>
