<form method="post" action="{{ path('app_transfer_departure_delete', {'id': transfer_departure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transfer_departure.id) }}">
    <button class="btn">Delete</button>
</form>
