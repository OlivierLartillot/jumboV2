{{ include('partials/header-reproupement.html.twig') }}

    {{ include('partials/page-title.html.twig', {pagetitle: 'Status', title: 'Status'}) }}

    <h1>Edit Status</h1>
    <a class="btn btn-secondary btn-sm my-3" href="{{ path('app_status_index') }}">back to list</a>

    {{ include('status/_form.html.twig', {'button_label': 'Update'}) }}


    {{ include('status/_delete_form.html.twig') }}

{{ include('partials/footer-regroupement.html.twig') }}
