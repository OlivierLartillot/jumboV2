<form method="post" action="{{ path('app_whats_app_message_delete', {'id': whatsAppMessage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this message ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ whatsAppMessage.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
